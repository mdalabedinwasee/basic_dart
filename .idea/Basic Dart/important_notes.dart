/* Serial list
  1.hello_world, 2.variables, 3.list_example, 4.set_example, 5.map_example, 6.constant, 7.operators, 8.type_casting, 9.string_interpolation,
   10., 11., 12., 13., 14., 15., 16., 17., 18.,
 */

// var / dynamic kokhono use kora jabe na.

// Single line comment korte surute //double forward slash// use korte hoy.

/*
Double line comment korte surute /single forward slash/ and *star* use korte hoy.
And last a *star* and /single forward slash/ use korte hoy.
*/

/*
# symbols name in programming language--
-> Assignment        : = (equals sign) assigns a value to a variable.
-> Comparison        : == (double equals sign) checks if two values are equal, while != (not equal) checks if they are not equal.
-> Arithmetic        : + (plus), - (minus), * (multiply), / (divide), % (modulo) perform mathematical operations.
-> Brackets          : [] (square brackets), () (parentheses), {} (curly braces), <> (Angle Brackets) are used for various purposes, including indexing arrays, defining function arguments, and grouping code blocks.
-> String Delimiters : "" (double quotes) and '' (single quotes) enclose text strings.
-> Semicolon         : ; indicates the end of a statement.
*/

/*
j j vabe lekha jai:
camelCase  (preferable)
snack_case
Pascal
Kebab-case
SCREAMING
 */